name: iris-grib-dev

channels:
  - conda-forge

dependencies:
  - python=3.11

# Setup dependencies.
  - setuptools

# Core dependencies.
  - iris>=3.0.2
  - python-eccodes>=1.6.1
  - eccodes>=2.32.1

# Optional dependencies.
  - mo_pack
  - pip

# Test dependencies.
  - mock
  - filelock
  - pep8
  - requests
  - nose

# Documentation dependencies.
  - sphinx 
  - sphinx_rtd_theme