name: iris-grib-dev

channels:
  - conda-forge

dependencies:
  - python=3.10

# Setup dependencies.
  - setuptools>=64
  - setuptools_scm>=8

# Core dependencies.
  - iris>=3.0.2
  - python-eccodes>=1.6.1
  - eccodes>=2.32.1

# Optional dependencies.
  - mo_pack
  - pip

# Test dependencies.
  - filelock
  - requests
  - iris-sample-data
  - pytest
  - pytest-mock

# Documentation dependencies.
  - sphinx
  - sphinx_rtd_theme
