name: iris-grib-dev

channels:
  - conda-forge

dependencies:
  - python=3.6

# Core dependencies.
  # Iris should be installed manually in dev mode - enables working with latest code.
  - python-eccodes

# Optional dependencies.
  - mo_pack

# Test dependencies.
  - mock
  - filelock
  - pep8
