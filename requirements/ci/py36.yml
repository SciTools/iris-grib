name: iris-grib-dev

channels:
  - conda-forge/label/rc_iris

dependencies:
  - python=3.6

# Setup dependencies.
  - setuptools

# Core dependencies.
  - iris=3.0.0rc0
  - python-eccodes

# Optional dependencies.
  - mo_pack

# Test dependencies.
  - mock
  - filelock
  - pep8
