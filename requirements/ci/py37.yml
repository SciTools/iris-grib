name: iris-grib-dev

channels:
  - conda-forge

dependencies:
  - python=3.7

# Setup dependencies.
  - setuptools

# Core dependencies.
  - iris>=3
  - python-eccodes

# Optional dependencies.
  - mo_pack

# Test dependencies.
  - mock
  - filelock
  - pep8
